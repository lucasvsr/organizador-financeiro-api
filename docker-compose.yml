version: '3.4'

services:

  db:
    image: postgres
    restart: always
    environment:
        - POSTGRES_PASSWORD=postgres
        - POSTGRES_USER=postgres
        - POSTGRES_DB=organizador_financeiro
    ports:
      - 5432:5432
